import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export const SaveWordsScreen = () => {
	return (
		<SafeAreaView>
			<Text>Save Words Screen</Text>
		</SafeAreaView>
	);
};
